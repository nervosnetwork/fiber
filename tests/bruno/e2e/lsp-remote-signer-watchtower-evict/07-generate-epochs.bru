meta {
  name: confirm hosted external funding
  type: http
  seq: 7
}

post {
  url: {{CKB_RPC_URL}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {"id":42,"jsonrpc":"2.0","method":"generate_epochs","params":["0x2"]}
}

script:pre-request {
  await new Promise(r => setTimeout(r, 3000));
}

assert {
  res.body.error: isUndefined
}

script:post-response {
  await new Promise(r => setTimeout(r, 3000));
}
