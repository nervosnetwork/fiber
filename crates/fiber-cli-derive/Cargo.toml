[package]
name = "fiber-cli-derive"
version = "0.1.0"
edition = "2021"
description = "Proc macro for generating CLI commands from fiber-json-types param structs"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full", "extra-traits"] }
